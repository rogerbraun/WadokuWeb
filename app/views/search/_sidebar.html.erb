<div id="search_box" class="block notice">
  <h3>Suche</h3>
  <div class="content">
    <p><%= t "search.info" %></p>
    <p>
      <%=form_tag search_index_path, :method => :get, :remote => true, :class => :form do %>
          <div class="group">
            <%= label_tag "search", t("search.any", :default => "Suchbegriff"),:class => "label" %>
            <%= text_field_tag "search", params[:search], :class => "text_field" %>
            <br />
          </div>

          <div class="group navform wat-cf">
            <button class="button" type="submit">
              <%= image_tag("web-app-theme/icons/tick.png", :alt => "#{t("search", :default => "Save")}") %> <%= t("search.search", :default => "Save") %>
            </button>  
          </div>
      <% end %>
    </p>
  </div>
</div>

<%= javascript_tag do %>
  $(window).scroll(function(){
    if($(window).scrollTop() > 100) {
      $("#search_box").animate({"margin-top": ($(window).scrollTop() - 100) + "px"}, {queue: false});
     }
    else
    {
      $("#search_box").animate({"margin-top": "0px"}, {queue: false});
    }
      
  });
<% end %>

